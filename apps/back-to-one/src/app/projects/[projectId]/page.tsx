// Hub content is rendered by the project layout (always mounted).
// This page intentionally returns null so the hub stays visible as the base layer.
export default function HubPage() {
  return null
}
