// Thread-system color tokens — see BRAND_TOKENS.md § Thread System.
// Fixed values. Never project-derived, never phase-derived. A thread surface
// uses only these; a project surface uses only project tokens.

export const TV = '#7C3AED'       // Thread violet — primary thread color
export const TA = '#F59E0B'       // Thread amber — unread state
export const TA_DEEP = '#D97706'  // Thread amber (deep) — unread badge background
