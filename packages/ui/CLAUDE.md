# @origin-one/ui

This package does one thing: provide the shared component library and design tokens.

## Rules
- Base components and tokens only
- No business logic
- No app-specific components
- All three apps must be able to import from here without conflict
