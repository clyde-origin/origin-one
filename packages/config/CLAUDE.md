# @origin-one/config

This package does one thing: provide shared config for all apps and packages.

## What lives here
- tsconfig.base.json — all apps extend from this
- Shared ESLint config
- Shared Prettier config

## Rules
- No code — config files only
- Changes here affect all three apps simultaneously
